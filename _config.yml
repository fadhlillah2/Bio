# GitHub Pages (Jekyll): keep build/reference files out of the published site.
# NOTE: the repo itself is public — this only stops these paths from being
# served at https://fadhlillah2.github.io/Bio/... ; they remain browsable in
# the Git repo. The LinkedIn source-of-truth snapshot (linkedin-profile-v4.txt)
# and a stray screenshot were removed from the working tree entirely.
exclude:
  - CLAUDE.md
  - cv/archive
  - cv/README.md
  - cv/build-pdf.py
